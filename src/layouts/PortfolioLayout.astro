---
import BaseLayout from '../layouts/PortfolioLayout_noheader.astro';

import Header from '../components/header/main.astro';

const { pageTitle, homebutton_hidden, page_url } = Astro.props;
---

<BaseLayout pageTitle={pageTitle} homebutton_hidden={homebutton_hidden} page_url={page_url}>
  <Header />
  <slot />
</BaseLayout>
